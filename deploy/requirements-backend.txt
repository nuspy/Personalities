# Dipendenze del backend. Volutamente senza torch: l'API e il worker CPU non
# addestrano nulla, e trascinare CUDA in un'immagine che serve richieste HTTP
# significherebbe gigabyte per nulla. Il worker GPU usa un'immagine propria.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy>=2.0
alembic>=1.14
psycopg[binary]>=3.2
# Il tipo `vector` per SQLAlchemy: senza, le colonne di embedding andrebbero
# scritte come SQL grezzo e l'autogenerazione di Alembic non le vedrebbe.
pgvector>=0.3
redis>=5.2
httpx>=0.27
python-jose[cryptography]>=3.3
opentelemetry-api>=1.28
opentelemetry-sdk>=1.28
opentelemetry-instrumentation-fastapi>=0.49b0
# Le query e le chiamate ai modelli: senza queste due, una traccia mostra la
# richiesta e nient'altro, proprio dove il tempo passa davvero.
opentelemetry-instrumentation-sqlalchemy>=0.49b0
opentelemetry-instrumentation-httpx>=0.49b0
opentelemetry-exporter-otlp-proto-http>=1.28
