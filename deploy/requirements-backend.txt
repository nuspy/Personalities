# Dipendenze del backend. Volutamente senza torch: l'API e il worker CPU non
# addestrano nulla, e trascinare CUDA in un'immagine che serve richieste HTTP
# significherebbe gigabyte per nulla. Il worker GPU usa un'immagine propria.
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy>=2.0
alembic>=1.14
psycopg[binary]>=3.2
# Il tipo `vector` per SQLAlchemy: senza, le colonne di embedding andrebbero
# scritte come SQL grezzo e l'autogenerazione di Alembic non le vedrebbe.
pgvector>=0.3
redis>=5.2
httpx>=0.27
# I caricamenti dalla console e la dettatura arrivano come multipart: senza,
# FastAPI rifiuta di registrare le rotte con `UploadFile` e l'API non parte.
python-multipart>=0.0.9
# I guardrail sono Markdown con frontmatter YAML, letti all'avvio.
pyyaml>=6.0
python-jose[cryptography]>=3.3
opentelemetry-api>=1.28
opentelemetry-sdk>=1.28
opentelemetry-instrumentation-fastapi>=0.49b0
# Le query e le chiamate ai modelli: senza queste due, una traccia mostra la
# richiesta e nient'altro, proprio dove il tempo passa davvero.
opentelemetry-instrumentation-sqlalchemy>=0.49b0
opentelemetry-instrumentation-httpx>=0.49b0
opentelemetry-exporter-otlp-proto-http>=1.28

# Trascrizione di audio e video. Non serve all'API — che non trascrive nulla —
# ma al worker che ingerisce i corpora: si installa con l'estra `ingestione`
# perche' trascina onnxruntime e ctranslate2, e su un nodo che serve solo
# richieste HTTP sarebbero centinaia di megabyte per niente.
# faster-whisper>=1.1      (installare sul worker di ingestione)
#
# Richiede anche ffmpeg nel PATH, che non e' un pacchetto Python:
#   Debian/Ubuntu: apt install ffmpeg
#   Windows:       scoop install ffmpeg
